import styled from 'styled-components'

export const TransparenceX = styled.div`
  width: inherit;
  height: inherit;
  background: linear-gradient(to top, var(--black) 10%, transparent 90%);
  display: flex;
  flex-direction: column;
  justify-content: center;
`

export const TransparenceY = styled.div`
  width: inherit;
  height: inherit;
  background: linear-gradient(to right, var(--black) 30%, transparent 70%);
  padding: 70px 0 150px 30px;
`
